// PaymentRequest was added in Chrome 60, not available in Chrome 50
{ const PaymentRequest = {}; }

// AbortController was added in Chrome 66, not available in Chrome 50
{ let AbortController = null; }

// IntersectionObserver was added in Chrome 51, not available in Chrome 50
{ var IntersectionObserver = function() {}; }
