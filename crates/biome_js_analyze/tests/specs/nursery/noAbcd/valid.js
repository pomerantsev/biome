/* APIs available in Chrome 50 - should not generate diagnostics */

// fetch was added in Chrome 42
const myFetch = fetch;

// MutationObserver was added in Chrome 18
let MutationObserver = null;

// URL was added in Chrome 19
var URL = "example";
